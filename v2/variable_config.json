{
  "_comment": "Security Gateway config. Precedence: env var (in [brackets]) > this file > default. Values shown are for instance SG1; a second instance (SG2) overrides gateway_code / db_name / ports / address / owner via env (or its own mounted config). The gateway calls the CA (OCSP + cert signing), TSA (timestamps) and Global Server (registration + global config).",

  "general": {
    "instance": "GOVSTACK"
  },

  "database": {
    "host": "localhost",
    "port": 3306,
    "user": "root",
    "password": "root"
  },

  "servers": {
    "ca_url": "http://127.0.0.1:9001",
    "tsa_url": "http://127.0.0.1:9002",
    "global_url": "http://127.0.0.1:9000"
  },

  "security_gateway": {
    "gateway_code": "SG1",
    "db_name": "sgateway_sg1_db",
    "port": 8081,
    "message_port": 8443,
    "address": "https://127.0.0.1:8443",
    "mtls": true,
    "auto_approve": false,
    "owner_class": "GOV",
    "owner_code": "1001",
    "owner_name": "Ministry of Digital Affairs",
    "title": "Security Gateway SG1 (cluster A)"
  }
}
